codecov:
  branch: stable

coverage:
  status:
    project:
      default: off
      dynamic_array:
        target: $COVERAGE_THRESHOLD
        flags:
          - dynamic_array

comment:
  layout: "diff, flags, files"
  behavior: default
  require_changes: false  
  require_base: yes
  require_head: yes     
  branches: null

flags:
  dynamic_array:
    paths: 
      - dynamic_array.h
    carryforward: false

